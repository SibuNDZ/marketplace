# Override docker-java's default Docker API version (1.32).
# Docker Desktop 29.x requires >= 1.40.  This classpath resource is loaded
# by DefaultDockerClientConfig.createDefaultConfigBuilder() before any
# environment or system-property overrides, so it is always applied.
api.version=1.44
